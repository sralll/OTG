{
  "name": "city-gen",
  "version": "0.1.0",
  "description": "Voronoi city/ward generator for sprint orienteering maps (port of TownGeneratorOS).",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node test/smoke.mjs",
    "serve": "python -m http.server 8000"
  }
}
